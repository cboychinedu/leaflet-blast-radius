// Importing the necessary modules 
import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

// Getting the DOM element for the root div 
const root = document.getElementById('root');

// Rendering the App component 
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>, 
  root
);
